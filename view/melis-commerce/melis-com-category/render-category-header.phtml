<?php 
    $id = '';
    if (!empty($this->zoneconfig['conf']['id'])){
        $id = 'id="'.$this->zoneconfig['conf']['id'] .'"';
    }
    $data_melisKey = 'data-melisKey="'.$this->melisKey.'"';
?>
<div class="card-header" style="height: initial;" <?php echo $id.' '.$data_melisKey; ?>>
	<div class="card-title" style="margin-top: inherit;">
		<div class="panel-heading-buttons">
    		<?php 
            	if(!empty($this->zoneconfig['interface'])) {
            	    foreach($this->zoneconfig['interface'] as $keyConfig => $menuConfig) {
            	        echo $this->$keyConfig;
            	    }
            	}
            ?>
        </div>
		<a class="accordion-toggle" data-bs-toggle="collapse" data-bs-target="#categoryInfoPanel">
			<h1 class="content-heading"><?php echo $this->escapeHtml($this->title) ?></h1>
		</a>
	</div>
</div>