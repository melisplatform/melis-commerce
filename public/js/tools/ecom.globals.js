var contactToolSelectedAccount = '';/*holds the selected account when linking/unlinking*/
var accountToolSelectedContact = '';/*holds the selected contact when linking/unlinking*/
var contactToolInitAccountAutoSuggest = false;/*use to check whether we need to re init the auto suggest of account tool when linking/unliking*/
var accountToolInitContactAutoSuggest = false;/*use to check whether we need to re init the auto suggest of contact tool when linking/unliking*/